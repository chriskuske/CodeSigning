Commit message:
feat: add CHANGELOG and make tool-download deterministic; add -UpdateTools switch and credential-manager clarifications

Summary:
- Add CHANGELOG.md describing recent changes.
- Make AzureSignTool/cosign download on-demand and respect -UpdateTools (avoid re-downloads when executables exist).
- Clarify credential manager behavior (metadata-only exports) and return empty arrays when no credentials exist.
- Include installer and GUI files. Update README.

Files to stage:
- CodeSignWrapper.ps1
- CredentialManager.ps1
- README.md
- Install-CodeSignWrapper.ps1
- CodeSignGui.ps1
- CHANGELOG.md
- COMMIT_MESSAGE.txt

Suggested git commands:
git add CodeSignWrapper.ps1 CredentialManager.ps1 README.md Install-CodeSignWrapper.ps1 CodeSignGui.ps1 CHANGELOG.md COMMIT_MESSAGE.txt
git commit -F COMMIT_MESSAGE.txt
git push origin <your-branch>
